package chaos.unity.loliasm.api;

/**
 * Class, methods that will potentially cause unsafe operations should be annotated with {@link Unsafe}, which users
 * should use it carefully.
 */
public @interface Unsafe {
}
